# ── 后端 API 地址（全局唯一配置点，代码里统一用 src/utils/apiBase.ts 读取）──
#
# 留空（推荐开发时用）：
#   前端请求走相对路径 /api/*，由 Vite 代理转发到 localhost:8080。
#   好处：手机连同一 Wi-Fi 通过局域网 IP 访问 H5 时也能正常调接口。
#
# 配置为完整地址（如 http://localhost:8080）：
#   前端直连后端。后端 CORS 已全开（AllowedOriginPattern("*")），跨域没问题。
#   注意：手机真机访问时不要配 localhost，要配电脑的局域网 IP，否则会指向手机自己。
#
VITE_API_BASE=
